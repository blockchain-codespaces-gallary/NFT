{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch frontend",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start all"
        },
    ]
}
